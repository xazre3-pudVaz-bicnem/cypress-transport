# ご用意いただきたい情報一覧

このサイトは「確認できていない情報は載せない」方針で作っています。
そのため、未確定の項目は **コード上で `null` にしてあり、画面にも構造化データにも一切出力されません**。

以下の情報をいただければ、該当ファイルの値を差し替えるだけでサイト全体（表示・構造化データ・sitemap）に反映されます。
**この作業のために、こちらで推測して値を埋めることは絶対にしません。**

優先度は次の基準です。

- **P1** … 公開前に埋めたい（信頼性・法令表示に直結）
- **P2** … 求人公開のタイミングで必須
- **P3** … あると強い（SEO・採用力が上がる）

---

## P1. 会社情報

編集ファイル: `data/site.ts` の `company`

| 項目 | 変数名 | 現在 | 備考 |
|---|---|---|---|
| 代表者名 | `representative` | 未設定 | 会社概要に表示 |
| 会社設立年月日 | `founded` | 未設定 | 例: `"2019年4月1日"` |
| 軽貨物事業部の開設時期 | `divisionEstablished` | 未設定 | 例: `"2026年9月"` |
| 法人番号（13桁） | `corporateNumber` | 未設定 | 国税庁法人番号公表サイトで確認できます |
| 郵便番号 | `address.postalCode` | 未設定 | 構造化データの `postalAddress` にも反映 |
| 資本金 | `capital` | 未設定 | 任意 |
| 電話受付時間 | `phoneHours` | 未設定 | 例: `"平日9:00〜19:00"`。応募者が一番気にする項目 |
| 営業時間 | `businessHours` | 未設定 | |
| 定休日 | `closedDays` | 未設定 | |
| 軽貨物事業部の所在地 | `divisionAddress` | 未設定 | **本社と異なる場合のみ**。同じなら不要 |
| コーポレートサイトURL | `corporateSiteUrl` | 未設定 | 株式会社サイプレス本体のサイト。あれば相互リンクで実在性が上がります |
| 公開用メールアドレス | `publicEmail` | 未設定 | 任意 |

### 貨物軽自動車運送事業の届出

| 項目 | 変数名 | 備考 |
|---|---|---|
| 届出番号・許可番号 | `businessLicense` | **運輸支局への届出が完了しているか**をまず教えてください。未了なら、完了後に記載します |

---

## P2. 求人情報

編集ファイル: `data/jobs.ts`

現在は `status: "draft"` のテンプレートが1件だけ入っており、**サイト上には表示されていません**（アクセスすると404）。

以下がすべて揃った時点で `status` を `"open"` に変えると、
求人一覧・個別求人ページ・JobPosting構造化データ・sitemap・サイト全体のCTA文言が**自動で切り替わります**。

| 項目 | 変数名 | JobPosting必須 |
|---|---|---|
| 求人タイトル | `title` | ✅ |
| 仕事内容（詳しく） | `description` | ✅ |
| 掲載開始日 | `datePosted` | ✅ |
| 募集終了日 | `validThrough` | ✅ |
| 契約形態（業務委託／雇用） | `employmentTypeLabel` / `employmentType` | ✅ |
| 報酬（表示文言＋金額データ） | `salary` | ✅ |
| 勤務地の詳細住所 | `workLocationDetail` | 推奨 |
| 配送エリア | `deliveryArea` | |
| 勤務時間 | `workHours` | |
| 稼働日数 | `workDays` | |
| 休日 | `holidays` | |
| 応募資格 | `requirements` | |
| 必要免許（AT限定の可否含む） | `licenses` | |
| 必要な経験 | `experience` | |
| 車両条件（持ち込み／リース／貸与） | `vehicle` | |
| 経費負担の区分 | `expenses` | |
| 待遇 | `benefits` | |
| 研修の有無と内容 | `training` | |
| 募集人数 | `headcount` | |
| 応募方法 | `applyMethod` | |
| フォームだけで応募が完結するか | `directApply` | `true` にすると Google 求人検索で「直接応募」扱いになります |

> ⚠️ **必須項目（✅）が1つでも欠けていると、JobPosting構造化データは意図的に出力されません。**
> 中途半端なデータで Search Console にエラーを出さないための安全装置です（`lib/jobs.ts` の `isJobPostingComplete`）。

### 特に確認したい点

現在サイト上で「当社の条件」として書いていない項目です。事実を教えていただければ記載します。

- 契約形態は**業務委託**か**雇用**か
- **未経験者**を受け入れるか
- **AT限定免許**で可か
- **車両を持っていない人**に、リース／貸与などの手段を用意するか
- **研修・同乗期間**があるか
- **副業・週数日**の稼働を受け入れるか
- ガソリン代・駐車場代・保険料を**誰が負担する**か
- 案件の種類（宅配／企業配／ルート／スポット）

---

## P3. 実在性を高める情報（SEO・採用に効きます）

### 代表メッセージ

編集ファイル: `data/site.ts` の `representativeMessage`

現在 `null` のため**セクションごと非表示**です。実際の言葉をいただければ会社概要に掲載します。
架空の文章を載せることはしません。

いただきたい内容の例:

- なぜ軽貨物事業を始めたのか
- どんなドライバーと働きたいか
- 事業をどう広げていきたいか

### 事業立ち上げの背景

編集ファイル: `data/site.ts` の `foundingStory`

**「なぜ葛飾区で軽貨物事業を始めたのか」は、他社が絶対に書けない一次情報です。**
検索でもAI検索でも、この手の内容が最も評価されます。ヒアリングさせてください。

### 実績の数字

編集ファイル: `data/site.ts` の `companyStats`

現在すべて `null` のため非表示です。**架空の数字は入れません。**
稼働が始まって実数が出たら教えてください。

- 登録ドライバー数 / 稼働ドライバー数 / 保有・稼働車両数 / 取引企業数 / 月間配送件数

### 写真（最優先で効く改善）

現在 `public/` にある写真は**すべてイメージカット**です。方針として、

- 人物が写ったAI画像は使用していません（架空の人物を「当社スタッフ」に見せないため）
- `alt` に実在の場所・人物を断定していません

以下を撮影いただければ差し替えます。**実写に変えることがE-E-A-T上いちばん効きます。**

- 実際に使用する軽貨物車両（外観・荷室・ナンバー部分は必要に応じて加工）
- 配送拠点・積み込みの様子
- 実際のドライバー（掲載可否の同意をいただけた方のみ）
- 事務所・看板

### 記事の執筆者・監修者

編集ファイル: `data/authors.ts`

現在は組織名義（編集部）のみです。実在しない人物は載せません。

- 記事を書く担当者の**氏名・経歴・担当業務**
- 法務／税務記事の**監修者**（行政書士・税理士など。依頼できれば E-E-A-T が大きく上がります）

---

## 公開前の環境変数（Vercel）

| 変数名 | 用途 | 必須 |
|---|---|---|
| `NEXT_PUBLIC_SITE_URL` | canonical・OG・sitemap の基準URL。本番は `https://cypress-transport.com` | 推奨（未設定でも本番ドメインにフォールバック） |
| `RESEND_API_KEY` | 問い合わせメール送信（Resend） | ✅ フォームを動かすなら必須 |
| `CONTACT_EMAIL_TO` | 問い合わせの受信先アドレス | ✅ |
| `CONTACT_EMAIL_FROM` | 送信元（Resendで認証したドメインのアドレス） | ✅ |
| `NEXT_PUBLIC_GA4_ID` | GA4 計測ID | 任意（未設定ならGA4のJSを一切読み込みません） |
| `GOOGLE_SERVICE_ACCOUNT_JSON_PATH` | Indexing API 用のサービスアカウント鍵のパス（**ローカル実行のみ**） | 求人公開時 |

> 🔒 サービスアカウント鍵（JSON）とAPIキーは **絶対にGitへコミットしないでください**。
> `.gitignore` で `google-service-account*.json` と `.env*` を除外済みです。

---

## ドメイン切り替え時の作業

現在 `cypress-transport.vercel.app` で稼働しています。本番ドメインに切り替える際は:

1. Vercel にカスタムドメイン `cypress-transport.com` を追加
2. www の有無を**どちらかに統一**（Vercel のリダイレクト設定）
3. `NEXT_PUBLIC_SITE_URL` を Production 環境に `https://cypress-transport.com` で設定
4. 再デプロイ（canonical・OG・sitemap・JSON-LD がすべて本番URLに切り替わります）
5. Search Console でドメインプロパティを登録し、sitemap を送信

> Vercel の Preview 環境は `robots.txt` で全ページ Disallow + `<meta name="robots" content="noindex">` を出力するため、
> プレビューURLがインデックスされることはありません（`app/robots.ts` / `app/layout.tsx`）。
